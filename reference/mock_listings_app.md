# Mock app without layout

`mock_listings_app` launches a mock app for the dv.listings shiny
module. Only for development purposes.

## Usage

``` r
mock_listings_app(mode = "single")
```

## Arguments

- mode:

  Character value that indicates whether to include one or multiple
  datasets. Could either be "single" or "multi".

## See also

Other mock apps:
[`mock_listings_mm()`](https://boehringer-ingelheim.github.io/dv.listings/reference/mock_listings_mm.md)
