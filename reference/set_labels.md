# Internal helper function to set labels for all dataset columns

Internal helper function to set labels for all dataset columns

## Usage

``` r
set_labels(dataset, labels)
```

## Arguments

- dataset:

  `[data.frame]`

  Single data.frame

- labels:

  Character vector of labels named with the respective column names
  `dataset`.

## Value

A data.frame with column labels
